name: usbip
variant: scratch
shell: /bin/bash
dependencies:
  - stage: base
  # Pull modules from YOUR custom kernel image.
  # If using the standard build system, this resolves via BUILD_ARG_PKGS.
  # If you built the kernel separately, replace with your image ref directly.
  - image: "{{ .BUILD_ARG_PKGS_PREFIX }}/kernel:{{ .BUILD_ARG_PKGS }}"
steps:
  - install:
      - |
        export KERNELRELEASE=$(find /usr/lib/modules -type d -name "*-talos" -exec basename {} +)
        mkdir -p /rootfs
        xargs -a /pkg/files/modules.txt -I {} install -D /usr/lib/modules/${KERNELRELEASE}/{} /rootfs/usr/lib/modules/${KERNELRELEASE}/{}
        depmod -b /rootfs/usr ${KERNELRELEASE}
  - test:
      - |
        # Verify all .ko files have valid signatures
        # https://www.kernel.org/doc/html/v4.15/admin-guide/module-signing.html#signed-modules-and-stripping
        find /rootfs/usr/lib/modules -name '*.ko' -exec grep -FL '~Module signature appended~' {} +
      - |
        mkdir -p /extensions-validator-rootfs
        cp -r /rootfs/ /extensions-validator-rootfs/rootfs
        cp /pkg/manifest.yaml /extensions-validator-rootfs/manifest.yaml
        /extensions-validator validate --rootfs=/extensions-validator-rootfs --pkg-name="${PKG_NAME}"
finalize:
  - from: /rootfs
    to: /rootfs
  - from: /pkg/manifest.yaml
    to: /
